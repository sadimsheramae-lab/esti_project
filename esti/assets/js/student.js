function printGrades(){window.print();}document.querySelectorAll('.sem-tab').forEach(tab=>{tab.addEventListener('click',()=>{document.querySelectorAll('.sem-tab').forEach(t=>t.classList.remove('active'));tab.classList.add('active');const sem=tab.dataset.sem;document.querySelectorAll('.sem-section').forEach(sec=>{sec.style.display=(sem==='all'||sec.dataset.sem===sem)?'':'none';});});});window.addEventListener('load',()=>{document.querySelectorAll('.grade-bar-fill').forEach(bar=>{const pct=bar.dataset.pct||'0';bar.style.width=pct+'%';});});const picInput=document.getElementById('profilePicInput');if(picInput){picInput.addEventListener('change',()=>{const file=picInput.files[0];if(file){const reader=new FileReader();reader.onload=e=>{const preview=document.getElementById('profilePreview');if(preview.tagName==='IMG'){preview.src=e.target.result;}else{const img=document.createElement('img');img.src=e.target.result;img.id='profilePreview';preview.replaceWith(img);}};reader.readAsDataURL(file);}});}document.querySelectorAll('.notif-item[data-id]').forEach(item=>{item.addEventListener('click',()=>{const id=item.dataset.id;fetch('mark_read.php?id='+id);item.classList.remove('unread');item.querySelector('.unread-dot')?.remove();const badge=document.querySelector('.notif-badge');if(badge){const n=parseInt(badge.textContent)-1;if(n<=0)badge.remove();else badge.textContent=n;}});});const pwField=document.getElementById('new_password');const strengthBar=document.getElementById('strengthBar');if(pwField&&strengthBar){pwField.addEventListener('input',()=>{const v=pwField.value;let score=0;if(v.length>=8)score++;if(/[A-Z]/.test(v))score++;if(/[0-9]/.test(v))score++;if(/[^A-Za-z0-9]/.test(v))score++;const colors=['#dc3545','#fd7e14','#ffc107','#28a745'];const labels=['Weak','Fair','Good','Strong'];strengthBar.style.width=(score*25)+'%';strengthBar.style.background=colors[score-1]||'#dee2e6';const lbl=document.getElementById('strengthLabel');if(lbl)lbl.textContent=labels[score-1]||'';});}
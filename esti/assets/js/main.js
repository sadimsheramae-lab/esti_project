const sidebar=document.getElementById('sidebar');const menuToggle=document.getElementById('menuToggle');const backdrop=document.getElementById('modalBackdrop');if(menuToggle){menuToggle.addEventListener('click',()=>sidebar.classList.toggle('open'));}function openModal(id){document.getElementById(id).classList.add('open');backdrop.classList.add('open');}function closeModal(id){document.getElementById(id).classList.remove('open');backdrop.classList.remove('open');}backdrop.addEventListener('click',()=>{document.querySelectorAll('.modal-backdrop.open').forEach(m=>m.classList.remove('open'));backdrop.classList.remove('open');});document.querySelectorAll('.alert').forEach(a=>{setTimeout(()=>a.style.display='none',4000);});function initSearch(inputId,tableId,columns){const input=document.getElementById(inputId);if(!input)return;input.addEventListener('keyup',()=>{const term=input.value.toLowerCase();document.querySelectorAll(`#${tableId}tbody tr`).forEach(row=>{const cells=row.querySelectorAll('td');const match=Array.from(cells).some((c,i)=>(!columns||columns.includes(i))&&c.textContent.toLowerCase().includes(term));row.style.display=match?'':'none';});updateCount(tableId);});}function initFilter(selectId,tableId,colIndex){const sel=document.getElementById(selectId);if(!sel)return;sel.addEventListener('change',()=>{const val=sel.value.toLowerCase();document.querySelectorAll(`#${tableId}tbody tr`).forEach(row=>{const cell=row.querySelectorAll('td')[colIndex];row.style.display=(!val||cell?.textContent.toLowerCase()===val)?'':'none';});updateCount(tableId);});}function updateCount(tableId){const visible=document.querySelectorAll(`#${tableId}tbody tr:not([style*="none"])`).length;const counter=document.getElementById('rowCount');if(counter)counter.textContent=`Showing ${visible}entries`;}function confirmDelete(form){if(confirm('Are you sure you want to delete this record?This action cannot be undone.')){form.submit();}}function drawDonut(canvasId,data,colors){const canvas=document.getElementById(canvasId);if(!canvas)return;const ctx=canvas.getContext('2d');const cx=canvas.width/2,cy=canvas.height/2;const r=Math.min(cx,cy)-10;const inner=r*0.6;const total=data.reduce((a,b)=>a+b,0);let angle=-Math.PI/2;data.forEach((val,i)=>{const slice=(val/total)*Math.PI*2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,r,angle,angle+slice);ctx.closePath();ctx.fillStyle=colors[i];ctx.fill();angle+=slice;});ctx.beginPath();ctx.arc(cx,cy,inner,0,Math.PI*2);ctx.fillStyle='#fff';ctx.fill();ctx.fillStyle='#343a40';ctx.font='bold 22px Segoe UI';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(total.toLocaleString(),cx,cy-8);ctx.font='11px Segoe UI';ctx.fillStyle='#6c757d';ctx.fillText('Students',cx,cy+12);}function drawLine(canvasId,labels,values){const canvas=document.getElementById(canvasId);if(!canvas)return;const ctx=canvas.getContext('2d');const W=canvas.width,H=canvas.height;const pad={top:20,right:20,bottom:40,left:50};const cW=W-pad.left-pad.right;const cH=H-pad.top-pad.bottom;const max=Math.max(...values)*1.15;const step=cW/(labels.length-1);ctx.clearRect(0,0,W,H);ctx.strokeStyle='#e9ecef';ctx.lineWidth=1;[0,.25,.5,.75,1].forEach(t=>{const y=pad.top+cH*(1-t);ctx.beginPath();ctx.moveTo(pad.left,y);ctx.lineTo(pad.left+cW,y);ctx.stroke();ctx.fillStyle='#6c757d';ctx.font='10px Segoe UI';ctx.textAlign='right';ctx.fillText(Math.round(max*t),pad.left-6,y+4);});ctx.textAlign='center';ctx.fillStyle='#6c757d';ctx.font='10px Segoe UI';labels.forEach((l,i)=>ctx.fillText(l,pad.left+i*step,H-10));const pts=values.map((v,i)=>({x:pad.left+i*step,y:pad.top+cH*(1-v/max)}));ctx.beginPath();ctx.moveTo(pts[0].x,pad.top+cH);pts.forEach(p=>ctx.lineTo(p.x,p.y));ctx.lineTo(pts[pts.length-1].x,pad.top+cH);ctx.closePath();const grad=ctx.createLinearGradient(0,pad.top,0,pad.top+cH);grad.addColorStop(0,'rgba(35,154,87,.35)');grad.addColorStop(1,'rgba(35,154,87,.02)');ctx.fillStyle=grad;ctx.fill();ctx.beginPath();ctx.strokeStyle='#239a57';ctx.lineWidth=2.5;pts.forEach((p,i)=>i===0?ctx.moveTo(p.x,p.y):ctx.lineTo(p.x,p.y));ctx.stroke();pts.forEach(p=>{ctx.beginPath();ctx.arc(p.x,p.y,4,0,Math.PI*2);ctx.fillStyle='#239a57';ctx.fill();ctx.strokeStyle='#fff';ctx.lineWidth=2;ctx.stroke();});}